protected-mode yes

# Disable direct replica takeover commands sent by untrusted clients
rename-command REPLICAOF ""
rename-command SLAVEOF ""

# Keep default persistence options; rely on docker volume /data
